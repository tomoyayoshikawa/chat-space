- @group.messages.each do |message|
 %ul_.messages__chat__user
  %li.messages__chat__user__name 
   = message.user.name
  %li.messages__chat__user__time 
   = message.created_at
 %p
  = message.body if message.body
 %p
  = image_tag(message.image) if message.image

